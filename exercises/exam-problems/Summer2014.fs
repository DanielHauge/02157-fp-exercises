
module Summer2014


module Summer2014Q1 = //1-3.1
    
    //1
    // f 2 3 : 2 * (2 * (2 * (2 * (1)) * 1) * 2) * 3)

    // g (fun x -> x>0) (fun x -> -x) [1;2;-3] : [-1;-2]
    // p fungere som et filter predikat på genrisk type a
    // f fungere som en map funktion fra type a til b
    // input er en list af en generisk type a
    // output er en liste af generisk type b

    // h (A 5) = "5"
    // h (C("hej","sa") = "hejsa"
    // h B("gg") = "gg"



    // f = int -> int -> int
    // Computes a number

    // g = (a' -> bool) -> (a' -> b') -> a' list -> b' list
    // Computes a filtered and formatted list. (Filter by predicate p and formats by function f)

    // h = T -> string
    // Computes a string, (computes the string representation of T)



    printf "hejsa"